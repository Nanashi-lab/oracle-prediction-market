# Sports Oracle Workflow
# EVM Log trigger — listens for SettlementRequested events, resolves via Gemini AI

staging-settings:
  user-workflow:
    workflow-name: "sports-workflow-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "sports-workflow-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"
