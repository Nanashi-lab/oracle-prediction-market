# AI News Resolver Workflow
# Uses Gemini AI with Google Search grounding to fact-check questions and resolve markets

staging-settings:
  user-workflow:
    workflow-name: "ai-news-workflow-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "ai-news-workflow-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"
