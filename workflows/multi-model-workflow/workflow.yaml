# Multi-Model AI Oracle Workflow
# Queries both Gemini and OpenAI — only settles when both models agree

staging-settings:
  user-workflow:
    workflow-name: "multi-model-workflow-staging"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "multi-model-workflow-production"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"
